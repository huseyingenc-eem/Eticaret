import { sidebarNav } from "./sidebar.config";
import { userMenuItems,topbarNav } from "./topbar.config";

const adminNavigation = {
    sidebar: sidebarNav,
    topbar: topbarNav,
    userMenuItems: userMenuItems,
};
export default adminNavigation;